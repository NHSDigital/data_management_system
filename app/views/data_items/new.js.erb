<%
  @node ||= @data_item
  html_content = escape_javascript bootstrap_form_for([@dataset_version, @data_item],
    url: dataset_version_data_items_path(@dataset_version), readonly: false,
    remote: true, horizontal: true) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Data Item');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
$(toggle_node_form_element('data-dictionary-element', 'add-dde'));
$(toggle_node_form_element('governance', 'add-governance'));
<%= render "nodes/save" %>
