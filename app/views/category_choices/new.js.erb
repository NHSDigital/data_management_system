<%
  @node ||= @category_choice
  html_content = escape_javascript bootstrap_form_for([@dataset_version, @category_choice],
    url: dataset_version_category_choices_path(@dataset_version),
    remote: true, horizontal: true) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Data Item');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
<%= render "nodes/save" %>
