<%= content_tag(:div, id: "#{dom_id(@commentable)}_comments") do %>
  <%= render 'form', comment: @comment %>
  <hr />
  <div class="comments-list">
    <% @comments.each do |comment| %>
      <%= render 'comment', comment: comment %>
    <% end %>
  </div>
<% end %>
