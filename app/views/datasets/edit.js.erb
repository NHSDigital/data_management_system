<%
  html_content = escape_javascript bootstrap_form_for(@dataset, remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html("Editing Dataset: <%= @dataset.name %>");
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
