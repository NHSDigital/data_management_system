var $modal = $("#modal");

$('.modal-title', $modal).text("Approve <%= j(ProjectNode.model_name.human) %>");
$('.modal-body', $modal).html(
  "<%= j(render 'approvals/form', model: @project, url: project_project_nodes_bulk_approval_path(@project)) %>"
);

$modal.modal('show');
