<%
  html_content = escape_javascript bootstrap_form_for([@team, @membership], remote: true, horizontal: false) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html("Edit <%= @membership.member_full_name %>'s membership");
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
