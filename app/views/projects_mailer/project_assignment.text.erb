<%
  scope    = @project.current_state.to_lookup_key
  title    = @project.name
  state    = @project.current_state_name
  assigner = @assigned_by&.full_name || t('system.name')
%>
##### <%= t(".#{scope}.heading", default: :'.heading', title: title, state: state) %> #####

<%= t(".#{scope}.body", default: :'.body', assigner: assigner) %>

<% if @comments %>
"<%= @comments %>"
— <%= assigner %>


<% end %>
Project Details:

<%= render 'project_details', project: @project %>

<%= render 'footer' %>
