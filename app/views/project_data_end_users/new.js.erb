<%
  html_content = escape_javascript bootstrap_form_for([@project, @project_data_end_user], remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Create a new Project data end user');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
