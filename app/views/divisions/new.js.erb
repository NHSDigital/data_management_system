<%
  html_content = escape_javascript bootstrap_form_for(@division, remote: true, horizontal: false) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Create a New Division');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
