<%
  html_content = escape_javascript bootstrap_form_for(@division, remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html("Editing Division: <%= @division.name %>");
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
