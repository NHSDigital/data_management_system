var $modal = $("#modal");

$('.modal-title', $modal).text("Reject project members");
$('.modal-body', $modal).html(
  "<%= j(render 'approvals/form', model: @project, url: project_members_rejection_path(@project)) %>"
);

$modal.modal('show');
