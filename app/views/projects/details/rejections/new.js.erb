var $modal = $("#modal");

$('.modal-title', $modal).text("Reject project details");
$('.modal-body', $modal).html(
  "<%= j(render 'approvals/form', model: @project, url: project_details_rejection_path(@project), tags: ['DetailRejection']) %>"
);

$modal.modal('show');
