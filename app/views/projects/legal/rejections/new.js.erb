var $modal = $("#modal");

$('.modal-title', $modal).text("Reject legal/ethical details");
$('.modal-body', $modal).html(
  "<%= j(render 'approvals/form', model: @project, url: project_legal_rejection_path(@project), tags: ['LegalRejection']) %>"
);

$modal.modal('show');
