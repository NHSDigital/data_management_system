<%
  html_content = escape_javascript bootstrap_form_for([@team, @project], horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Create a New Project');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
