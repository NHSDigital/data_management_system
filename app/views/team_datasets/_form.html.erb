<%= bootstrap_form_for [@team, @team_dataset], remote: true, horizontal: 2 do |form| %>
  <%= form.control_group :dataset_id, 'Dataset' do %>
    <%= form.select :dataset_id, @team.data_sources_not_in_use.collect {|ds| [ ds.name, ds.id ] }, { include_blank: @team.data_source_prompt }, disabled: @team.disable_data_source_dropdown? %>
  <% end %>

  <%= form.control_group nil do %>
    <div class='pull-right'>
      <%= close_modal %>
      <%= submit_tag('Save', class: 'btn btn-primary', disable_with: 'Saving&hellip;'.html_safe) %>
    <div>
  <% end %>
<% end %>
