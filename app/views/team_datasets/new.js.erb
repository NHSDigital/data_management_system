<%
  html_content = escape_javascript bootstrap_form_for([@team, @team_dataset], remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Data source to Team');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
