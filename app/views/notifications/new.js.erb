
<%
  html_content = escape_javascript bootstrap_form_for([@notification], remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Create broadcast notification for all users');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
