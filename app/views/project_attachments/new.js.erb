<%
  html_content = escape_javascript bootstrap_form_for([@project, @project_attachment], :html => {:multipart => 'true'}, remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('<%=project_attachment_header%>');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
