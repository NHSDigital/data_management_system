<%
  html_content = escape_javascript bootstrap_form_for(@team, remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html("Editing Team: <%= @team.name %>");
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
