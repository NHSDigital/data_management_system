<%
  html_content = escape_javascript bootstrap_form_for([@project, @project_data_source_item], remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Project data item');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
