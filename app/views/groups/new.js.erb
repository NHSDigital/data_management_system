<%
  @node ||= @group
  html_content = escape_javascript bootstrap_form_for([@dataset_version, @group],
    url: dataset_version_groups_path(@dataset_version),
    remote: true, horizontal: true) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Group');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
<%= render "nodes/save" %>
