<%
  html_content = escape_javascript bootstrap_form_for(
    [@dataset_version, @category], remote: true, horizontal: true) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add a new Category');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
