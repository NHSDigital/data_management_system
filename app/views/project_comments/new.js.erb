<%
  html_content = escape_javascript bootstrap_form_for([@project, @project_comment], remote: true, horizontal: 2) { |form| render('form', form: form) }
%>

jQuery('#modal_title').html('Add Comment to project');
jQuery('#modal_body').html('<%= html_content %>').parents('.modal').modal('show');
